# Runtime mode (필수)
NODE_ENV=development

# Server port (필수)
PORT=4000

# Allowed web origin for CORS (필수)
WEB_ORIGIN=http://localhost:3000

# Public origin used to build absolute upload URLs (필수)
SERVER_PUBLIC_ORIGIN=http://localhost:4000

# Database placeholders (추후 연동 예정)
DB_HOST=localhost
DB_PORT=5432
DB_USER=soondungi
DB_PASSWORD=soondungi
DB_NAME=soondungi

# MinIO placeholders (추후 연동 예정)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minio
MINIO_SECRET_KEY=minio123
MINIO_BUCKET=soondungi-dev
