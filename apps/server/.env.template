# Runtime mode
NODE_ENV=development

# Server port
PORT=4000

# Allowed web origin for CORS
WEB_ORIGIN=http://localhost:3000

# Public origin used to build absolute upload URLs.
# In production, set this to your real server origin.
SERVER_PUBLIC_ORIGIN=http://localhost:4000
