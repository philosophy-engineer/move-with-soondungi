# Runtime mode (필수)
NODE_ENV=development

# Server port (필수)
PORT=4000

# Allowed web origin for CORS (필수)
WEB_ORIGIN=http://localhost:3000

# Public origin used to build absolute URLs (필수)
SERVER_PUBLIC_ORIGIN=http://localhost:4000

# S3-compatible storage (필수)
S3_ENDPOINT_URL=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=minio
S3_SECRET_ACCESS_KEY=minio123
S3_BUCKET=soondungi-dev
S3_FORCE_PATH_STYLE=true
S3_PUBLIC_BASE_URL=http://localhost:9000/soondungi-dev

# PostgreSQL 18 (필수)
DB_HOST=localhost
DB_PORT=5432
DB_USER=soondungi
DB_PASSWORD=soondungi
DB_NAME=soondungi
