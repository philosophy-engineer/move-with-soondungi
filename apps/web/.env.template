# Runtime mode (usually set automatically by Next.js runtime/script)
NODE_ENV=development

# API server base URL
# Development default is http://localhost:4000 when unset.
# In production, this value is required.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
